# Tesseract 4.0 source file

### Cmake building official Tesseract source file within Visual Studio 2017 and Win 10 enviroment.

### You may need to add files in these directory `include` , `bin` , `lib` to reference and work Tesseract successfully. 

# Tesseract 4.0 source file

### Files are made by cmake building official Tesseract source file within Visual Studio 2017 and Win 10 enviroment.

### You may need to add files in these directory `include` , `bin` , `lib` to reference if you want to use Tessearct directly. 
